# Hugo build output directory
/public/

# Hugo resources directory (cache for processed images, SCSS, etc.)
/resources/_gen/
.hugo_build.lock

# Environment & Secrets
.env
.env.*
!.env.example

# Node / Frontend assets (if using PostCSS, Tailwind, npm packages)
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# OS metadata
.DS_Store
Thumbs.db

# IDE & Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Temporary / Log files
*.log
